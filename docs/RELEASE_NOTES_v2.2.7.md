## 随心起卦 v2.2.7

### 更新
- **应用内差量更新**（electron-updater）：安装版启动检测 → 应用内下载（优先 blockmap 差量）→ 一键重启安装，不再另开 Setup 与进程打架
- 便携版仍走完整安装包兜底

### 安装包
请上传：`exe` + `.blockmap` + **`latest.yml`**（自动更新必需）

| 平台 | 文件 |
|------|------|
| Windows 安装 | `suixin-qigua-2.2.7-win-x64.exe` |
| Windows 便携 | `suixin-qigua-2.2.7-win-portable.exe` |
| 更新元数据 | `latest.yml` + `*.blockmap` |
