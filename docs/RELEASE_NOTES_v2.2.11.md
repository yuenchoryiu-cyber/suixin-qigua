## 随心起卦 v2.2.11

### 更新
- **下载更新包 → 打开更新包**：应用内下载后，点「打开更新包」仅静默更新程序（不进完整 Setup 向导）
- 同时写入「下载」文件夹：`随心起卦-更新-x.y.z.exe` + `随心起卦-打开更新-x.y.z.cmd`（双击 cmd 也是静默更新）
- 跳版本、差量失败回退整包等逻辑保持

请上传：`exe` + `.blockmap` + `latest.yml`
